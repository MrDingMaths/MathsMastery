import { BaseLevel } from './BaseLevel.js';

export default new BaseLevel(
    'polynomialQuotientRule',
    'Polynomial Functions — Quotient Rule',
    [],
    { mode: 'derivative', toleranceDp: 2 }
);
