[multiplyTerms] — Easy: two factors, at most one negative, all degree-1, at most two distinct variables. Medium: two factors both negative (sign-change), OR three factors with ≤2 distinct letter variables, degree-1. Hard: three factors with ≥3 distinct letter variables, OR two/three factors with at least one powered term (degree ≥2).
[addSubtractTerms] — Easy: 2–3 terms, one like-term group, at most one constant bystander, no double-negative constructions. Medium: 3–4 terms with two distinct like-term groups (e.g. x and y), or negative-coefficient results, or commutativity recognition in product variables (ba=ab). Hard: mixed powers of same base (x² and x together), compound power variables (x²y, xy²), double-negative subtraction, 5+ terms across 3+ groups, or hidden cancellation in a complex expression.
[divideTerms] — Easy: 0 index-law reductions, ≤2 distinct variables (coefficient reduction + cancelling at most one shared first-power variable; sign/negatives not a difficulty driver). Medium: exactly 1 index-law reduction (one powered variable reduced, e.g. a²/a) OR 0 reductions but 3+ distinct variables/factors to track. Hard: 2 simultaneous index-law reductions — both numerator and denominator carry powers on two different variables, leaving variables top and bottom plus a coefficient fraction.
[mixedSimplification] — Easy: single-variable, single-operation-type, ≤3 additive terms all same variable, no two-variable products. Medium: two-variable monomials (xy/ab) introduced, OR four-term two-group expressions with no commutativity recognition required, OR two-monomial multiplication with mixed variables. Hard: commutativity recognition (yx=xy) required across like terms, OR three-factor chains, OR 5+ terms across 3+ groups, OR expressions mixing x²y with xy², OR fractions with sign-flip partial cancellation, OR compound multi-variable exponents on both variables simultaneously.
[powerOfPowerAndZeroPower] — Easy: single application of one law (power-of-a-power or zero power) on one variable or numeric base, no coefficient, no combination with other index laws. Medium: coefficient present (stays outside or gets raised with the monomial), or power-of-a-power combined with product/quotient law in two steps, or zero power embedded in an expression requiring arithmetic evaluation. Hard: two or more separately-powered monomials requiring simultaneous coefficient arithmetic and exponent arithmetic, or three-variable products with independent exponent calculations, or multi-step fractions combining power-of-a-power with coefficient reduction on both sides.
[divisionIndexLaw] — Easy: single variable or numerical base, no coefficient, one subtraction step giving a positive integer power result. Medium: coefficient simplifies to a clean integer, and/or two variables each requiring independent index subtraction; inverse cases and simple negatives with clean integer coefficients included. Hard: coefficient division produces a non-integer fraction, OR three distinct variables, OR double negatives, OR negative combined with fractional coefficient or inverse exponent case.
[multiplicationIndexLaw] — Easy: pure index law on a single letter variable, no numerical coefficients, just add exponents, any number of factors. Medium: two factors with explicit numerical coefficients and/or a second distinct letter variable; negative coefficients allowed. Hard: three or more factors with at least one coefficient or multi-variable term; OR fraction coefficients; OR three or more distinct letter variables; OR reordering/commutativity recognition required (e.g. ab×ba).
[expandSingleBrackets] — Easy: positive numeric or positive degree-1 monomial outer factor × two-term bracket, single variable family throughout (no cross-product terms). Medium: negative outer factor (sign-flip required), or two distinct variables producing cross-product terms, or three-term bracket; outer factor degree ≤ 1. Hard: fraction outer coefficient requiring integer simplification, or degree-2+ outer factor (x², -3y²) producing cubic+ results, or variable outer factor × bracket with degree-2 terms.
[mixedIndexLaws] — Easy: apply one index law (×, ÷, power-of-power, zero power) to a single-base expression with no coefficients. Medium: chain two or more laws on a single base with no coefficients (e.g. expand then divide, or multiply then divide). Hard: apply index laws to expressions with numeric coefficients and/or multi-variable monomials, including algebraic fractions or two or more power-of-power applications in the same expression.
[expandBinomialProducts] — Easy: both factors are monic linear binomials (coefficient 1 on the variable), numeric constants only, no scalar outside. Medium: scalar multiplier outside the product, OR at least one non-monic bracket, OR two monic products combined. Hard: two distinct letter variables in the product, OR abstract letter coefficients, OR variable monomial factor producing a cubic, OR two non-monic products combined.
[factoriseIntoSingleBrackets] — Easy: two terms, positive HCF; HCF is either a positive integer or a single degree-1 variable (coefficient 1), no sign management. Medium: two terms; HCF is a monomial with both a coefficient >1 and a variable (e.g. 3x), OR a negative leading term requiring sign-flip. Hard: three or more terms, OR HCF contains two or more distinct letter variables (e.g. ab), OR any variable in the HCF has degree ≥ 2.
[orderOfOperations] — Easy: exactly one × or ÷ evaluated before + or −; degree-1 terms only; at most one subsequent like-term collection. Medium: two or more sequential BODMAS choices required; bracket-vs-no-bracket contrast pairs, fractions, or cross-variable products; degree-1 in problem (degree-2 may appear in answer). Hard: at least one powered term (degree ≥ 2) in the problem; index law applied within the BODMAS chain; multi-variable with exponents or power-of-power.
[expandAndSimplify] — Easy: one bracket with positive outer factor expanded and collected with at most two like-term groups; single variable, no sign-flip from bracket negation. Medium: two degree-1 outer brackets combined with at most one negative outer factor (single variable), or one bracket with a negative outer factor. Hard: two brackets both with negative outer factors, or any expression producing degree-2+ terms in the result, or two-variable cross-bracket like-term collection, or three or more separate expressions combined.
[factoriseMonicQuadraticTrinomials] — Easy: all-positive monic quadratic (b>0, c>0 in x²+bx+c); factor pair search entirely in positive integers; no sign management. Medium: monic quadratic with at least one negative sign; sign-aware factor pair selection required to identify which root is negative. Hard: non-monic leading coefficient with a common integer factor k>1; student must extract k first to reveal k(x²+bx+c), then factorise the inner monic quadratic; inner quadratic may have any sign pattern.
[powerOfProductsAndQuotients] — Easy: single bracket with at most one powered variable inside (or two plain variables); at most one side of a quotient carries an inner exponent. Medium: single bracket with two or more bases each carrying inner exponents, OR a negative base, OR an external factor that merges via index laws after expansion. Hard: two or more separately-raised brackets combined by multiplication, division, or addition/subtraction, requiring cross-bracket index-law simplification to reach the final form.
[addSubtractAlgebraicFractions] — Easy: both fractions share the same numeric denominator; add/subtract numerators directly, simplifying the result if needed. Medium: different numeric denominators (or an integer constant combined with a fraction); find LCD to combine; all variables appear in numerators only. Hard: at least one denominator contains a variable or product of variables, OR two different letter variables appear requiring cross-multiplication, OR denominators mix different powers of the same variable.
[negativeIndices] — Easy: single-step direct application of a^{-n} = 1/aⁿ on one base at a time — isolated single-variable, coefficient × variable, numeric base, or fraction-with-negative-denominator expressions; no index law operations required. Medium: two or more bases with mixed positive/negative indices, or a single application of one index law (×, ÷, or power-of-power) where negative indices appear before or after; includes whole-expression negation like (kx)^{-n}. Hard: two or more sequentially applied index laws in a single expression — multi-step chains combining products, quotients, and powers of bracketed monomials across multiple variables, including fractions-of-fractions and nested powers with negative exponents at each stage.
[expandDifferenceOfTwoSquares] — Easy: coefficient-1 variable and integer constant in each factor; variable or constant may appear first. Medium: at least one term carries a coefficient ≠ 1, higher power, surd, or variable product; both squared terms expand cleanly to integers or rationals. Hard: at least one term is a fraction, reciprocal, or composite binomial expression used as a single term, so squaring requires fraction arithmetic or leaves an unexpanded binomial square.
[expandPerfectSquares] — Easy: single-variable monic linear binomial with integer constant; direct (a+b)² with whole-number arithmetic only. Medium: at least one term has a non-unit integer coefficient, two distinct letter variables appear, or a scalar multiplier sits outside the bracket; all bases are integer-coefficient polynomials. Hard: at least one term is a fraction or reciprocal (variable in denominator), or both terms are degree-≥2 polynomials across two distinct letter variables; requires fraction arithmetic or nested index-law bookkeeping.
[multiplyDivideAlgebraicFractions] — Easy: single × or ÷ with coefficient arithmetic only; no variable cancellation across fractions; degree-1 variables in both inputs and output. Medium: at least one variable cancellation across the two fractions required, OR the output has a squared variable arising from two degree-1 inputs multiplying the same variable; no degree ≥ 2 variable in any input. Hard: degree ≥ 2 variable present in at least one input fraction, OR three or more fractions combined in a chain, OR parenthetical grouping that changes the order of × and ÷.
[addSubtractFractionsWithBinomialNumerator] — Easy: two fractions, numeric denominators, addition only (or subtracting a monomial-only numerator); at least one numerator is a binomial; no sign-distribution challenge. Medium: subtraction requires distributing a negative across a full binomial second numerator, OR three fractions with at least one binomial numerator, OR two-variable binomials. Hard: at least one denominator contains a variable factor; LCD is algebraic, producing polynomial terms in the combined numerator.
[mixedExpansion] — Easy: one expansion technique applied once to integer-coefficient factors; result degree ≤ 3; single variable (or the most basic two-variable forms like (x+y)²); no post-expansion collection beyond the direct expansion. Medium: outer integer scalar multiplied across a binomial product k(ax+b)(cx+d), or two separately-expanded brackets combined and collected, or a triple product monomial × binomial × binomial, or a two-variable binomial × binomial with integer coefficients; no fractional coefficients within factors. Hard: factors involve fractional coefficients (including reciprocal terms 1/x), general literal coefficients (ax+b)(cx+d), or special cubic identities; OR multi-step compositions producing degree ≥ 4 results; OR combinations of three or more bracket groups with two-variable outputs requiring multi-stage collection.
[factorisePerfectSquares] — Easy: monic trinomials (leading coefficient 1) in a single variable, standard descending order; student identifies integer root n where middle coefficient = 2n and constant = n². Medium: non-monic trinomials where the leading term is (ax)² with a>1 (single or two variables), or two-variable monic trinomials (x±y)²; student must extract the square root of the leading coefficient or track two variables; includes reversed-order monic presentations. Hard: higher-degree substitution required (treating xⁿ as the base variable), OR fractional/reciprocal coefficients, OR mixed terms involving 1/x.
[factoriseDifferenceOfTwoSquares] — Easy: single-step DOTS on already-perfect-square terms (integer, integer-coefficient monomial, or two-variable); constant-first order allowed; no preliminary steps. Medium: one extra step required — extract a common integer factor, recognise a bracketed binomial as a squared term, handle fractional terms with rational square roots, or work with even powers ≥4 yielding polynomial roots. Hard: two or more sequential DOTS applications (fourth-power chains), OR at least one squared constant is non-perfect-square requiring surd simplification, OR both squared terms are binomial expressions whose post-DOTS product must be expanded and collected.
[noticeBinomialFactors] — Easy: both terms share an identical explicitly-written binomial factor; factor it out directly with no sign or power manipulation. Medium: the shared binomial is disguised by commutativity, opposite sign, or appears squared in one term, or a scalar GCF must be extracted alongside the binomial. Hard: at least one term has the binomial raised to a power >1 so the minimum-power factor is the HCF; surds, parametric outer coefficients, or multi-compound-bracket structures add further steps.
[groupInPairs] — Easy: all-positive coefficients, terms in natural pairing order, GCF per pair immediately visible. Medium: at least one negative coefficient or terms out of natural order, all coefficients numeric. Hard: abstract letter-parameters used as coefficients, or a common monomial factor spans all four terms.
[factoriseNonMonicQuadraticTrinomials] — Easy: positive leading coefficient with exactly one monic bracket (leading coefficient lands in a single factor), or a perfect-square/DOTS special form; no common factor, no leading negative. Medium: composite positive leading coefficient distributing across both non-monic factors, or a negative leading coefficient requiring −1 extracted first; primitive trinomial, includes irreducible recognition cases. Hard: a numeric common factor k>1 (possibly negative) must be extracted before factorising the inner quadratic.
[factoriseWithIndices] — Easy: all bases reduce to a single prime base via one-to-two index-law steps (×, ÷, power-of-power), or a direct one-step common-factor extraction from a sum/difference of same-base exponentials. Medium: compound non-prime bases (6, 12, 15, 18 …) requiring prime decomposition, or fraction simplification where both numerator and denominator are same-base exponential sums/differences needing common-factor extraction, or mixed-base factoring (e.g. 6^n − 3^n). Hard: multi-base compound fractions leaving two or more distinct prime bases in the result, or algebraic-style factoring in exponents (DOTS, perfect-square trinomial), or fraction simplification where the shared cancel factor is an algebraic expression rather than a pure power.
[mixedFactorisation] — Easy: single technique directly applied (common factor, DOTS, perfect square, or monic quadratic), single variable, integer coefficients, technique immediately identifiable. Medium: two-step or disguised factorisation — common factor + second technique, grouping by pairs, or binomial expression as a DOTS term; non-monic quadratics and rational-coefficient perfect squares allowed; no abstract parameters. Hard: three or more nested techniques, abstract literal parameters, variables in denominators, irrational/surd roots, repeated DOTS on degree ≥4, or algebraic rearrangement needed to reveal the form.
[compoundFractions] — Easy: single direct cancellation of a shared algebraic factor; no fraction arithmetic; result is a pure number or monomial. Medium: multiply by a single LCD to clear all nested fractions in one step; no polynomial factorisation; denominators are numeric or single-variable monomials. Hard: two or more non-trivial steps required — clearing then factorising a polynomial denominator (DOTS), distributing negative-index products, three-level continued fractions, or multiplying by x² to expose factorisable quadratics.
[finishFactorising] — Easy: one bracket holds a single integer GCF to extract (other factor already fully factored), or scalar × DOTS requiring one application; no further factorisation of resulting brackets. Medium: GCF from two separate brackets simultaneously, OR a bracket is a factorisable quadratic (monic, perfect-square, or DOTS creating a repeated factor); at most one technique per bracket. Hard: quadratic bracket requires GCF extraction then a second factorisation of the inner expression, OR regrouping/grouping-by-grouping needed before bracket techniques apply, OR DOTS on a squared binomial with multi-step scalar bookkeeping.
[simplifyAlgebraicFractionsByFactorising] — Easy: monomial common-factor extraction only (no quadratic factorisation); cancelable factor is a monomial, an explicitly-written binomial, or an opposite-sign pair. Medium: at least one side requires monic quadratic factorisation (trinomial, perfect square, or DOTS) to reveal the cancelable factor; all leading coefficients ±1. Hard: non-unit leading coefficient on at least one quadratic (non-monic factorisation required), OR degree ≥ 3 polynomial factorisation needed (cubic, grouping, higher-order DOTS), OR surd coefficients in the cancel factor.
[multiplyDivideAlgebraicFractionsByFactorising] — Easy: fractions are pre-factored or require at most one simple factorisation step (common factor or DOTS) in one fraction; single × or ÷ only. Medium: both fractions require factorising monic quadratic trinomials before cancellation; single × or ÷ only. Hard: three or more fractions in a chain, OR two distinct letter variables in the factorisations, OR at least one non-monic quadratic (leading coefficient ≠ 1) requiring the ac-method.
[addSubtractFractionsByFactorisingDenominator] — Easy: both denominators already in factored form (single linear or monomial); LCD is their direct product; sign-flip (a−b) = −(b−a) allowed but no polynomial factorisation required. Medium: at least one denominator must be factorised (common factor, DOTS, or monic trinomial) to reveal the LCD; or both denominators are pre-factored products requiring careful factor-list comparison to avoid double-counting. Hard: both denominators are unfactorised polynomials that must each be factorised and their factored forms share at least one common linear factor; or three+ fractions with multiple factorisations required; or cubic/higher-degree denominators.
[rationaliseBinomialDenominator] — Easy: integer or single unrelated surd as numerator over one binomial (integer ± surd) denominator; one conjugate step, no further simplification. Medium: coefficient-times-surd or full binomial numerator, or both numerator and denominator each contain two distinct surd terms; one conjugate step produces a multi-term surd numerator. Hard: sum or difference of two separately-rationalised fractions, nested compound fractions, or expressions with abstract algebraic letter parameters.
[rationaliseTheDenominator] — Easy: monomial numerator over plain √n denominator (no coefficient); one multiply step, no prior simplification needed. Medium: denominator carries a numeric coefficient k√n OR has a composite radicand (√8, √18…) requiring simplification; monomial numerator only. Hard: binomial or multi-term numerator requiring distribution after multiplying by √n/√n; composite-surd simplification may also be needed within the numerator terms.
[expandBracketsWithSurds] — Easy: outer factor is a non-negative monomial and bracket has two terms; DOTS on conjugate pairs with no pre-simplification required. Medium: negative or coefficient-surd outer factor; FOIL on non-conjugate surd binomials; perfect squares with surd terms; single expand-then-collect multi-step. Hard: all four FOIL cross-products are distinct surd products requiring independent simplification; fractional/reciprocal surd factors; three-bracket products or cubic powers; multi-step chains combining two or more expansions.
[multiplyDivideSurds] — Easy: two plain bare surds (no integer coefficient outside the radical) multiplied or divided; result is an integer, simple surd, or rational. Medium: at least one integer coefficient k outside a surd (k√a) present, requiring coefficient arithmetic; includes (k√a)^n powers and nested √(fraction) forms. Hard: two or more separately-raised surd powers combined in a multi-step chain requiring cross-cancellation, OR surd addition/subtraction as a required preliminary step before squaring or multiplying.
[addSubtractSurds] — Easy: all terms share a single surd radicand after at most one simplification; integer coefficients, ≤3 additive terms, single surd or integer result. Medium: two or more distinct surd radicands coexist in the expression or result, or multiple surds each require independent simplification; integer coefficients throughout. Hard: at least one surd term carries a fractional coefficient requiring LCD arithmetic alongside surd simplification, or algebraic letter bases inside radicals require variable cancellation.
[simplifySurds] — Easy: purely numeric radicand ≤ 100; single-step perfect-square extraction or direct (√n)² / k√(perfect square) evaluation; no outer coefficient combined with a non-trivial simplification. Medium: purely numeric; radicand > 100 or outer coefficient k≥2 paired with a non-trivial simplification, or (k√n)² with k>1; no variables. Hard: algebraic (variable) surds requiring index-law manipulation, surd fraction/quotient forms, products under a single radical, (k√n)²×m multi-step chains, or cube/higher roots.
[splittingTheNumerator] — Easy: both numerator and denominator monic; quotient always 1; constant arithmetic only. Medium: denominator monic, numerator leading coefficient a ≠ 1 (including a = −1); two-step arithmetic to find quotient then remainder. Hard: denominator non-monic (leading coefficient c > 1); must divide leading coefficients to find quotient, then multiply back for remainder.
[completeTheSquare] — Easy: monic quadratic (a=1) with even linear coefficient; halving b yields an integer so the completed form (x+n)²+k has all-integer parts with no fraction arithmetic. Medium: non-monic (|a|≥2 or a=−1) where b/(2a) is an integer; factor out a first then complete with integer internal halving, giving a(x+n)²+k with no fractions. Hard: non-monic (|a|≥2) where b/(2a) is not an integer; the bracket contains a fractional term (x+p/q)² requiring fraction arithmetic; the final constant may be integer or fractional.
[evaluateFractionalIndices] — Easy: base is a positive integer; index is a unit positive fraction (1/n); exactly one root-extraction step yields a positive integer result. Medium: two-step evaluation — compound positive index a^{m/n} (root then power), negative unit-fraction index a^{-1/n} (root then reciprocate), fractional or decimal base with unit-fraction index, or negative integer base with an odd unit root. Hard: three-step combination of root, power, and reciprocal — integer base with negative compound index a^{-m/n}, fractional base with any compound or negative index (p/q)^{±m/n}, or negative integer base with a compound index.
[surdFormToIndexForm] — Easy: single conversion step applying one rule to one base at a time (ⁿ√(xᵐ)=x^(m/n) or k/xⁿ=kx^(−n)); scalar coefficient present but does not combine with the base. Medium: two combined rules in one expression — negative fractional index from a fraction-over-root, exponent addition/subtraction from multiplying/dividing a surd with an integer-power factor, or two-variable perfect-power evaluation. Hard: three or more simultaneously combined bases with independent index arithmetic, multi-step nested chains mixing several surd and power terms, or same-base combining where the integer coefficient = base^k (k≥2).
